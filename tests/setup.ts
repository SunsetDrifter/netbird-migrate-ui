// Vitest global setup. Currently no-op — kept as a single hook point.
// MSW server boot lives in lib/netbird-client.test.ts where it's used,
// to keep node-env tests free of unrelated overhead.
export {};
